# Le serveur n'a plus besoin de clé API Anthropic : chaque utilisateur saisit
# la sienne directement dans l'application (stockée uniquement sur son appareil).
# Voir README.md.

# Port d'écoute du serveur (optionnel)
PORT=3000
