# Clé API Anthropic (Claude) requise pour le résumé et le fact-checking.
# Créez une clé sur https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Modèle Claude à utiliser (optionnel, valeur par défaut ci-dessous)
CLAUDE_MODEL=claude-sonnet-5

# Port d'écoute du serveur (optionnel)
PORT=3000
